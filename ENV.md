# Project Python environment

Use this when running Python in the terminal (e.g. tests, `main.py`).  
**Edit this file** with the command or path for your actual environment so the Cursor agent uses it.

## Option A – venv in this repo

If you use a venv in the project:

```bash
source /home/japhet/python_envs/rl_boardgames/bin/activate
# or:  source venv/bin/activate
```

Then run: `python test_rl.py` (or `python main.py`, etc.)
